"""Atlas governance subsystem (Volume 9).

Owns: DecisionValidator, AnswerGovernor, EvidenceStore, ConfidenceModel,
PolicyEngine, ClaimExtractor, EnforcementOrchestrator.

Governance schemas (GovernedOutput, ExtractedClaim, etc.) are the
authoritative definitions — imported from governance.schemas.
"""
